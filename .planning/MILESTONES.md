# Project Milestones: scan-erc20-token

## v1.0.4 ERC20 Transfer Baseline (Shipped: 2026-03-12)

**Delivered:** Stable ERC20 transfer scanning with block filtering, direction filters, proxy support, and TypeScript declarations.

**Phases completed:** Legacy pre-GSD work (phase history not tracked)

**Key accomplishments:**
- Published `scanErc20Transfers(...)` for EVM wallet ERC20 activity
- Added optional `tokenAddress` and `direction` filtering
- Added proxy, timeout, and injected-provider support
- Added README examples and mocked automated tests

**Stats:**
- 5 files modified in the `v1.0.3` → `v1.0.4` range
- 50 lines added, 12 lines removed in the final patch release diff
- GSD phases not tracked for pre-planning releases
- Same-day patch cadence between `v1.0.3` and `v1.0.4`

**Git range:** `v1.0.3` → `v1.0.4`

**What's next:** Add native-token scanning for EVM networks in `v1.0.5`

---
