---
phase: 03-docs-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - test/scan-native.test.js
autonomous: true
requirements:
  - DOC-02
user_setup: []
must_haves:
  truths:
    - Automated tests explicitly cover incoming, outgoing, and default-range native scans with mocked providers through the public package API.
    - Native verification remains offline and does not require a live RPC endpoint or real wallet data.
    - Release-readiness checks pass for the updated package surface, including runtime tests and TypeScript compile verification.
  artifacts:
    - path: test/scan-native.test.js
      provides: Explicit public-API test matrix for native transfer behavior
      min_lines: 140
      contains: scanNativeTransfers
  key_links:
    - from: test/scan-native.test.js
      to: src/index.js
      via: public native runtime verification through package exports
      pattern: scanNativeTransfers
    - from: test/scan-native.test.js
      to: README.md
      via: release-facing native behavior examples stay aligned with tested semantics
      pattern: latest-100-block fallback window
---

<objective>
Finalize the native-transfer verification matrix and release-readiness checks.

Purpose: Make the new native feature demonstrably covered by mocked tests before release-facing docs are considered complete.
Output: A clearer native test matrix through the public API and passing release-readiness verification commands.
</objective>

<execution_context>
@/Users/tonamson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-docs-verification/03-01-SUMMARY.md
@README.md
@test/scan-native.test.js
@src/index.js
@src/index.d.ts
@typecheck.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the native test matrix release-facing and explicit</name>
  <files>test/scan-native.test.js</files>
  <action>Refactor or extend `test/scan-native.test.js` so the required native scenarios are obvious at a glance: incoming scans, outgoing scans, and default-range scans through the public `scanNativeTransfers(...)` API with mocked providers. Keep the suite offline, preserve the Phase 1 and Phase 2 behavior contracts, and tighten naming/coverage where needed so the README examples and the test matrix tell a coherent story.</action>
  <verify><automated>node --test test/scan-native.test.js</automated></verify>
  <done>`test/scan-native.test.js` clearly covers the release-critical native scenarios through the public API.</done>
</task>

<task type="auto">
  <name>Task 2: Run release-readiness verification for docs-facing native support</name>
  <files>test/scan-native.test.js</files>
  <action>Use the updated native test matrix as the anchor for Phase 3 verification. Ensure the package-level test run and TypeScript compile still pass after the docs-facing cleanup, and keep the suite grounded in mocked provider data only. Do not introduce live RPC checks or flaky network dependencies.</action>
  <verify><automated>npm test && npm run typecheck</automated></verify>
  <done>Release-readiness verification passes for native runtime tests and TypeScript compile checks.</done>
</task>

</tasks>

<verification>
- [ ] `node --test test/scan-native.test.js` passes
- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] Native incoming, outgoing, and default-range scenarios are explicit in the mocked public-API test matrix
</verification>

<success_criteria>

- All tasks completed
- Native incoming, outgoing, and default-range behavior are clearly covered by mocked tests
- Verification remains offline and deterministic
- The package is ready for milestone-level completion work after Phase 3 execution
  </success_criteria>

<output>
After completion, create `.planning/phases/03-docs-verification/03-02-SUMMARY.md`
</output>
