---
phase: 03-docs-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
user_setup: []
must_haves:
  truths:
    - README shows how to call `scanNativeTransfers(...)` from the package root for both JavaScript and TypeScript consumers.
    - README states the milestone scope boundaries for native scanning, especially that only top-level native transfers are supported and internal trace transfers are out of scope.
    - README keeps the existing ERC20 guidance intact while adding native API notes for provider reuse, default block behavior, and result shape.
  artifacts:
    - path: README.md
      provides: Public usage guide for native transfer scanning and scope notes
      min_lines: 140
      contains: scanNativeTransfers
  key_links:
    - from: README.md
      to: src/index.js
      via: documented package-root native runtime API
      pattern: scanNativeTransfers
    - from: README.md
      to: src/index.d.ts
      via: documented public native types and result shape
      pattern: NativeTransfer
---

<objective>
Document the new native-transfer capability in the public README without overstating scope.

Purpose: Make the package understandable to end users now that native scanning is part of the public API.
Output: README sections and examples for native scanning, provider reuse, result shape, and explicit scope boundaries around top-level transfers only.
</objective>

<execution_context>
@/Users/tonamson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-public-api-typings/02-VERIFICATION.md
@README.md
@src/index.js
@src/index.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add native scan usage docs to README</name>
  <files>README.md</files>
  <action>Update `README.md` so the package documentation covers both ERC20 and native-transfer scanning. Add JavaScript and TypeScript examples using `scanNativeTransfers(...)`, show the native result shape with `direction`, and document that the function is imported from the package root alongside `scanErc20Transfers(...)`. Keep the existing ERC20 documentation usable; this task is an additive docs pass, not a README rewrite.</action>
  <verify><automated>node --input-type=module -e "import { readFileSync } from 'node:fs'; const readme = readFileSync('README.md', 'utf8'); for (const snippet of ['scanNativeTransfers', 'NativeTransfer', 'direction', 'top-level native transfers']) { if (!readme.includes(snippet)) throw new Error('Missing README snippet: ' + snippet); }"</automated></verify>
  <done>`README.md` includes public native scan usage and result examples alongside the existing ERC20 documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Document native scope boundaries and provider notes</name>
  <files>README.md</files>
  <action>Extend the README notes/API sections so users can see the default block behavior for native scans, provider reuse patterns, and the milestone's explicit boundaries. State plainly that native scanning covers top-level value-bearing transactions available through standard RPC and does not include internal trace transfers. Keep the wording release-facing and avoid research-heavy or chain-specific detail.</action>
  <verify><automated>node --input-type=module -e "import { readFileSync } from 'node:fs'; const readme = readFileSync('README.md', 'utf8'); for (const snippet of ['createRpcProvider', 'latestBlock - 100', 'internal trace transfers']) { if (!readme.includes(snippet)) throw new Error('Missing README note: ' + snippet); }"</automated></verify>
  <done>The README now explains native-scan boundaries, defaults, and provider reuse clearly enough for release consumers.</done>
</task>

</tasks>

<verification>
- [ ] README shows `scanNativeTransfers(...)` examples for JS and TS users
- [ ] README states top-level-only native scope and excludes internal trace transfers
- [ ] README documents native default block behavior and provider reuse patterns
</verification>

<success_criteria>

- All tasks completed
- README documents the native-transfer API added in Phase 2
- Scope boundaries are explicit enough that users will not infer support for internal trace transfers
- Existing ERC20 docs remain intact while native docs are added cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/03-docs-verification/03-01-SUMMARY.md`
</output>
