---
phase: 01-native-scan-core
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - test/scan-native.test.js
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
  - NATIVE-03
user_setup: []
must_haves:
  truths:
    - Mocked tests prove incoming and outgoing native scan behavior for positive-value top-level transfers.
    - Mocked tests prove default block window and invalid-range behavior stay aligned with the ERC20 scan rules.
    - Mocked tests prove failed transactions, zero-value transactions, and self-transfer duplication follow the locked Phase 1 rules.
  artifacts:
    - path: test/scan-native.test.js
      provides: Native scan behavior coverage using deterministic mock providers
      min_lines: 80
      contains: scanNativeTransfersWithProvider
  key_links:
    - from: test/scan-native.test.js
      to: src/native.js
      via: direct relative import for package-internal testing
      pattern: ../src/native.js
---

<objective>
Add deterministic coverage for the native-transfer core behavior.

Purpose: Lock the Phase 1 semantics with offline tests before the public API and typings are added in later phases.
Output: A focused native transfer test file that exercises incoming, outgoing, default-range, and filtering edge cases against the internal module.
</objective>

<execution_context>
@/Users/tonamson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-native-scan-core/01-CONTEXT.md
@test/scan-erc20.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build native scan mock-provider coverage</name>
  <files>test/scan-native.test.js</files>
  <action>Create `test/scan-native.test.js` and import `scanNativeTransfersWithProvider` directly from `../src/native.js` so Phase 1 can stay package-internal. Add mock provider helpers for block-number fallback, block retrieval with transaction lists, and receipt lookups. Cover incoming and outgoing positive-value top-level native transfers, including contract-call transfers that should still count as native movement.</action>
  <verify><automated>node --test test/scan-native.test.js</automated></verify>
  <done>The new test file proves the internal native scan returns incoming and outgoing native transfers using deterministic provider fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Lock edge-case rules for native scan behavior</name>
  <files>test/scan-native.test.js</files>
  <action>Extend the same test file with assertions for the Phase 1 edge rules: default latest-100-block fallback, `fromBlock > toBlock` rejection, failed receipt exclusion, zero-value exclusion, and self-transfer duplication into two records when `direction="both"`. Keep assertions explicit about `direction` values and block/timestamp data so future API wiring cannot silently change the core semantics.</action>
  <verify><automated>node --test test/scan-native.test.js</automated></verify>
  <done>The native transfer core behavior is protected by deterministic tests for all locked Phase 1 edge cases.</done>
</task>

</tasks>

<verification>
- [ ] `node --test test/scan-native.test.js` passes
- [ ] Tests cover incoming, outgoing, and self-transfer behavior with explicit `direction` assertions
- [ ] Tests confirm block fallback and filtering parity with the Phase 1 context
</verification>

<success_criteria>

- All tasks completed
- `test/scan-native.test.js` exists with substantive native scan coverage
- Tests guard the locked semantics for contract-call transfers, failed/zero-value filtering, and self-transfer duplication
- Phase 1 behavior can be safely wrapped by later phases without semantic drift
  </success_criteria>

<output>
After completion, create `.planning/phases/01-native-scan-core/01-02-SUMMARY.md`
</output>
