---
phase: 01-native-scan-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/native.js
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
  - NATIVE-03
user_setup: []
must_haves:
  truths:
    - Incoming top-level native transfers with positive value are returned for the wallet within the requested block range.
    - Outgoing top-level native transfers with positive value, including contract calls, are returned for the wallet with explicit direction metadata.
    - Native scan block defaults and invalid-range validation match the existing ERC20 scan behavior.
  artifacts:
    - path: src/native.js
      provides: Internal native transfer scan logic for provider-driven block inspection
      min_lines: 60
      exports:
        - scanNativeTransfersWithProvider
      contains: direction
  key_links:
    - from: src/native.js
      to: provider.getBlockNumber
      via: latest-block fallback resolution
      pattern: getBlockNumber
    - from: src/native.js
      to: provider.getBlock
      via: block iteration with transaction inspection
      pattern: getBlock
    - from: src/native.js
      to: provider.getTransactionReceipt
      via: receipt status filtering before mapping results
      pattern: getTransactionReceipt
---

<objective>
Build the internal native-transfer scan core used by Phase 1.

Purpose: Land the provider-driven native scan behavior without expanding the public package API before Phase 2.
Output: A package-internal `src/native.js` module that scans top-level native transfers with the agreed direction and block-range semantics.
</objective>

<execution_context>
@/Users/tonamson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-native-scan-core/01-CONTEXT.md
@src/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement provider-driven native scan module</name>
  <files>src/native.js</files>
  <action>Create `src/native.js` with an internal `scanNativeTransfersWithProvider(options)` export that accepts `wallet`, `direction`, `fromBlock`, `toBlock`, and an injected provider. Implement the Phase 1 rules exactly: inspect top-level transactions with `value > 0`, treat positive-value contract calls as valid native transfers, filter out failed transactions and zero-value transactions, duplicate self-transfers into both `in` and `out` records when `direction="both"`, and include an explicit `direction` field in each returned record. Preserve ERC20 parity for block fallback and `fromBlock > toBlock` validation. Do not add trace/internal-transfer support and do not wire this module into the public package exports yet.</action>
  <verify><automated>node --input-type=module -e "import('./src/native.js').then((m) => { if (typeof m.scanNativeTransfersWithProvider !== 'function') throw new Error('scanNativeTransfersWithProvider export missing'); })"</automated></verify>
  <done>`src/native.js` exports internal native scan logic that matches the locked Phase 1 semantics and keeps public package API changes out of scope.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize native transfer records and provider expectations</name>
  <files>src/native.js</files>
  <action>In the same module, define the internal record mapping and provider interaction contract so Phase 2 can wrap it cleanly. Returned records should stay close to the ERC20 shape by including `from`, `to`, `amount`, `tx`, `block`, `blockTimestamp`, `transactionIndex`, and `direction`, while intentionally omitting transaction `status` and any `isContractInteraction` flag. Document expected provider methods in code comments or JSDoc where the contract is non-obvious, including the need for block retrieval with transactions and receipt lookup for success filtering.</action>
  <verify><automated>node --input-type=module -e "import('./src/native.js').then(async ({ scanNativeTransfersWithProvider }) => { const provider = { async getBlockNumber(){ return 100; }, async getBlock(blockNumber, includeTransactions){ return { number: blockNumber, timestamp: 1, transactions: [] }; }, async getTransactionReceipt(){ return { status: 1 }; } }; const result = await scanNativeTransfersWithProvider({ provider, wallet: '0x1000000000000000000000000000000000000001', direction: 'both' }); if (!Array.isArray(result)) throw new Error('expected array result'); })"</automated></verify>
  <done>The internal native scan output shape is stable, minimal, and ready for Phase 2 public API wrapping without contradicting Phase 1 decisions.</done>
</task>

</tasks>

<verification>
- [ ] `node --input-type=module -e "import('./src/native.js')"` succeeds
- [ ] Internal native scan logic enforces Phase 1 direction, block-range, and filtering rules
- [ ] No public export or declaration changes are introduced in this phase
</verification>

<success_criteria>

- All tasks completed
- Internal native scan logic exists in `src/native.js`
- The implementation honors the locked context decisions for transfer source, filtering, and block-range behavior
- Phase 1 core logic remains package-internal until Phase 2
  </success_criteria>

<output>
After completion, create `.planning/phases/01-native-scan-core/01-01-SUMMARY.md`
</output>
